[package]
name = "newlaw"
version = "0.1.2"
description = "NEWLAW desktop app"
authors = ["lucashungaro"]
edition = "2021"

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-updater = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
tauri-build = { version = "2", features = [] }
